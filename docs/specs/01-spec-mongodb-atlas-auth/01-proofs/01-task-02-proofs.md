# Task 2.0 Proof Artifacts

## Code: User Model Implementation

**File**: `models/User.ts`

```typescript
import mongoose, { Document, Model, Schema } from 'mongoose';

// TypeScript interface for User document
export interface IUser extends Document {
  googleId: string;
  email: string;
  name: string;
  image: string;
  createdAt: Date;
  updatedAt: Date;
}

// Mongoose schema definition
const UserSchema = new Schema<IUser>(
  {
    googleId: {
      type: String,
      required: [true, 'Google ID is required'],
      unique: true,
      index: true,
    },
    email: {
      type: String,
      required: [true, 'Email is required'],
      unique: true,
      lowercase: true,
      trim: true,
      match: [
        /^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/,
        'Please provide a valid email address',
      ],
    },
    name: {
      type: String,
      required: [true, 'Name is required'],
      trim: true,
    },
    image: {
      type: String,
      required: false,
      trim: true,
    },
  },
  {
    timestamps: true, // Automatically adds createdAt and updatedAt fields
  }
);

// Prevent model recompilation in Next.js hot reload
const User: Model<IUser> =
  mongoose.models.User || mongoose.model<IUser>('User', UserSchema);

export default User;
```

## TypeScript: Type Safety Verification

### Interface Definition
The `IUser` interface extends Mongoose's `Document` type and defines all fields with proper TypeScript types:
- `googleId: string` - Google OAuth identifier
- `email: string` - User email address
- `name: string` - User display name
- `image: string` - User profile image URL
- `createdAt: Date` - Timestamp auto-generated by Mongoose
- `updatedAt: Date` - Timestamp auto-generated by Mongoose

### Type Safety Features
1. **Strongly typed model**: `Model<IUser>` ensures all database operations are type-safe
2. **Document extension**: Extends Mongoose `Document` for full Mongoose method typing
3. **Schema type parameter**: `Schema<IUser>` ensures schema matches interface

## Code: Validation Rules and Data Integrity

### Unique Constraint on googleId
```typescript
googleId: {
  type: String,
  required: [true, 'Google ID is required'],
  unique: true,  // ✓ Unique constraint
  index: true,   // ✓ Database index for performance
}
```

### Email Validation
```typescript
email: {
  type: String,
  required: [true, 'Email is required'],
  unique: true,
  lowercase: true,  // ✓ Normalize to lowercase
  trim: true,       // ✓ Remove whitespace
  match: [
    /^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/,  // ✓ Email format validation
    'Please provide a valid email address',
  ],
}
```

### Required Fields
All critical fields are marked as required:
- `googleId`: `required: [true, 'Google ID is required']`
- `email`: `required: [true, 'Email is required']`
- `name`: `required: [true, 'Name is required']`
- `image`: Optional field (can be null)

### Automatic Timestamps
```typescript
{
  timestamps: true, // ✓ Automatically adds createdAt and updatedAt
}
```

## Verification Summary

✅ **Mongoose schema created** with all required fields (googleId, email, name, image)
✅ **TypeScript interface** (`IUser`) provides type safety for all operations
✅ **Unique constraint** on `googleId` field prevents duplicate users
✅ **Email validation** with regex pattern and normalization (lowercase, trim)
✅ **Required field validation** on googleId, email, and name
✅ **Automatic timestamps** (createdAt, updatedAt) enabled via schema options
✅ **Next.js compatibility** with model recompilation prevention
✅ **Database indexing** on googleId for query performance

## File Location
- **Model**: `/Users/jburns/git/five31/models/User.ts`
- **Status**: Created and ready for integration
